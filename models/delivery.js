class delivery  {
 
 
 
 
    constructor(id, ownerId, orden, deliveryID, action,  name_origin, phone_origin, description_product, quantity, amount, weight, dimensions, address, municipio, zone, indications, location, state_delivery, deliveryDestino, typeDestino, driver, googledata, totaldistance, totalduration) {
      this.id = id;
      this.ownerId = ownerId;
      this.orden = orden;
      this.deliveryID = deliveryID;
      this.action = action;
      this.name_origin = name_origin;
      this.phone_origin = phone_origin;
      this.description_product = description_product;
      this.quantity = quantity;
      this.amount = amount;
      this.weight = weight;
      this.dimensions = dimensions;
      this.address = address;
      this.municipio = municipio;
      this.zone = zone;
      this.indications = indications;
      this.location = location;
      this.state_delivery = state_delivery;
      this.deliveryDestino = deliveryDestino;
      this.typeDestino = typeDestino;
      this.driver = driver;
      this.googledata = googledata;
      this.totaldistance = totaldistance;
      this.totalduration = totalduration;
    }
  }
   export default delivery;
  
  
 
 